# ON:DA.LAB 제작 정책

## 변경 보호 원칙 — 최우선·필수

**사용자가 변경을 요청한 항목 외에는 AS-IS, 즉 직전 버전을 반드시 그대로 유지한다.**

- 사용자의 요청 목록을 `변경 허용 목록`으로 간주하고, 그 목록에 필요한 최소 범위만 수정한다.
- 요청하지 않은 문구, 콘텐츠, DOM 구조, 디자인, 레이아웃, 반응형 동작, 인터랙션, 애니메이션, 접근성 속성, 이미지, 파일명, 경로, 설정과 기능을 임의로 변경하지 않는다.
- 사용자가 요청하지 않은 정리, 개선, 재설계, 문장 다듬기, 리팩터링, 이름 변경, 의존성 갱신과 이미지 재생성을 하지 않는다.
- 요청한 변경 때문에 연관 수정이 불가피할 때는 필요한 최소 범위만 수정한다. 가시적 결과나 기존 기능에 추가 영향을 주는 경우에는 먼저 사용자에게 확인한다.
- 사용자 제공 원본 이미지는 명시적 교체 요청이 없는 한 생성 이미지, 추출 이미지, 유사 이미지 또는 저화질 이미지로 대체하지 않는다.
- 작업 완료 전 전체 diff를 요청 목록과 대조한다. 요청과 무관한 변경은 제거하고, 요청하지 않은 영역이 직전 버전과 동일한지 확인한다.

이 원칙은 미관 개선, 코드 정리, 추정한 의도와 작업 편의보다 우선한다.

## AI 협업 투명성 — 필수

ON:DA.LAB에서 서비스하거나 공개하는 모든 웹페이지에는 `AI 협업 투명성`을 반드시 표시한다.

페이지 또는 산출물마다 다음 네 항목을 실제 제작 과정에 맞게 작성한다.

1. **AI 사용 범위** — 조사, 초안, 구조화, 이미지 생성, 코딩, 자동화 등 AI가 담당한 구체적 범위
2. **원본 출처** — 직접 작성, 고객 제공, 기관 제공, 공개자료 등 원본 콘텐츠의 출처
3. **사람의 검토자** — 사실 확인, 수정, 최종 판단과 공개 승인을 담당한 사람
4. **공개 표시** — 내부용, AI 보조 제작물, AI 생성 포함, 공개 가능 등 현재 공개 상태

### 구현 원칙

- 페이지 하단 또는 주요 정보 영역에 네 개의 요약 배지를 노출한다.
- 상세 네 항목은 항상 읽을 수 있어야 하며, 접기/펼치기 UI를 사용할 수 있다.
- 기본 요약 배지는 `AI 보조`, `원본 출처`, `Human Reviewed`, `공개 상태` 순서로 사용한다.
- AI가 사용되지 않은 항목은 임의로 채우지 않고 `사용하지 않음`으로 명시한다.
- 사람의 최종 검토와 공개 책임을 AI에 귀속시키지 않는다.
- 실제 제작 과정과 다른 범위·출처·검토 상태를 표시하지 않는다.

세부 문안과 예시는 `docs/AI_COLLABORATION_TRANSPARENCY.md`를 따른다.
